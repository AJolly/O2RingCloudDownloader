[project]
name = "o2downloader"
version = "0.1.0"
description = "Viatom/Wellue O2Ring Data Downloader"
dependencies = [
    "requests",
    "numpy",
    "plotly",
    "pandas",
]

[tool.uv]
package = false
